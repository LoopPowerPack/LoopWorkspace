{
  "latest": "0.3.11",
  "notes": "Site Atlas can now pop up automatically after you change your pod or CGM sensor, so you never forget to log the new site. Turn it on in Settings > Site Atlas > \"Prompt After Site Change\" (off by default). The prompt waits until you're back on the main screen, so it never interrupts pod pairing. Also fixes an issue where the site logging prompt only appeared if the Site Atlas screen happened to be open.",
  "url": "https://github.com/LoopPowerPack/LoopWorkspace"
}
